[project]
name = "ninesui"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.11.4",
    "textual>=3.2.0",
]

[dependency-groups]
dev = [
    "textual-dev>=1.7.0",
]
